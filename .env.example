# Plausible Analytics — opt-in (leave commented to disable telemetry)
# Required: domain + endpoint. Optional: token (Plausible /api/event is normally unauthenticated; self-hosted setups may require Bearer)
# PLAUSIBLE_DOMAIN=plan-pipeline-v4.example
# PLAUSIBLE_ENDPOINT=https://plausible.io/api/event
# PLAUSIBLE_API_TOKEN=__YOUR_TOKEN_HERE__   # optional
